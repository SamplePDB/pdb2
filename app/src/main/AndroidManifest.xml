<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reachuson.app.pdb2">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/pdb2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pdb2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".Register" />
        <activity android:name=".Verify" />
        <activity android:name=".Verify2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Doc" />
        <activity android:name=".Retailer" />
        <activity android:name=".Pharmacy"></activity>
    </application>

</manifest>